# AUDIT REFERENCE: Sections 10.3, 13.6
# STATUS: NEW
name: Build BionicSX2 IPA

on:
  push:
    branches: [main, ios-port]
  pull_request:
    branches: [main]

env:
  CMAKE_BUILD_PARALLEL_LEVEL: 8

jobs:
  build:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Xcode CLI tools
        run: xcode-select --install || true

      - name: Cache iOS dep sources
        uses: actions/cache@v4
        with:
          path: ios-deps/src
          key: ios-deps-src-v1

      - name: Cache iOS dep builds
        uses: actions/cache@v4
        with:
          path: ios-deps/install
          key: ios-deps-install-${{ hashFiles('scripts/build_ios_deps.sh') }}

      - name: Clone iOS Dependencies
        run: |
          mkdir -p ios-deps/src
          cd ios-deps/src

          [ -d zlib ]     || git clone --depth 1 \
            https://github.com/madler/zlib.git

          [ -d zstd ]     || git clone --depth 1 \
            https://github.com/facebook/zstd.git

          [ -d lz4 ]      || git clone --depth 1 \
            https://github.com/lz4/lz4.git

          [ -d xz ]       || git clone --depth 1 \
            https://github.com/tukaani-project/xz.git

          [ -d freetype ] || git clone --depth 1 \
            https://gitlab.freedesktop.org/freetype/freetype.git

          [ -d libzip ]   || git clone --depth 1 \
            https://github.com/nih-at/libzip.git

      - name: Patch libzip for iOS (memcpy_s compat header)
        run: |
          DIR="ios-deps/src/libzip/lib"
          FILE="$DIR/zip_buffer.c"
          if [ -f "$FILE" ]; then
            printf '#pragma once\n#include <string.h>\n#include <stddef.h>\n' > "$DIR/zip_compat_ios.h"
            printf 'static inline int _zip_memcpy_s(void *d, size_t ds, const void *s, size_t c) {\n' >> "$DIR/zip_compat_ios.h"
            printf '  (void)ds; memcpy(d, s, c); return 0;\n}\n' >> "$DIR/zip_compat_ios.h"
            printf '#define memcpy_s(d,ds,s,c) _zip_memcpy_s(d,ds,s,c)\n' >> "$DIR/zip_compat_ios.h"
            sed -i.bak '1s/^/#include "zip_compat_ios.h"\n/' "$FILE"
            echo "Patched $FILE with zip_compat_ios.h"
          fi

      - name: Build third-party deps for iOS
        run: |
          chmod +x scripts/build_ios_deps.sh
          bash scripts/build_ios_deps.sh

      - name: Configure BionicSX2
        run: |
          cmake -S . -B build-ios \
            -DCMAKE_TOOLCHAIN_FILE=cmake/ios.toolchain.cmake \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_QT=OFF \
            -DENABLE_OPENGL=OFF \
            -DENABLE_VULKAN=OFF \
            -DUSE_SDL=OFF \
            -DBUILD_TESTS=OFF

      - name: Build Core Library
        run: |
          cd build-ios
          make BionicSX2 2>&1
          echo "Core exit code: $?"

      - name: Build App Target
        run: |
          cd build-ios
          make BionicSX2_App 2>&1
          echo "App exit code: $?"

      - name: Create IPA
        run: |
          mkdir -p Payload
          cp -r build-ios/BionicSX2_App.app Payload/
          zip -r BionicSX2.ipa Payload/
          echo "IPA size: $(du -sh BionicSX2.ipa)"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: BionicSX2-IPA
          path: BionicSX2.ipa
          retention-days: 7
